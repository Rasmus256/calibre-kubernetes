apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: calibre-books
    namespace: calibre
spec:
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 1Gi
    storageClassName: local-path
    volumeName: calibre-books-2
